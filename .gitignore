# Ignore the virtual environment
venv/

# Also ignore the __pycache__
__pycache__

# You can also put .vscode in as well. That is for visual studio code settings
.vscode

# Hide the .env file because that is where we keep our secrets, keys, etc.
.env 

# Ignore the SQLite Database (for development/testing use only)
app.db